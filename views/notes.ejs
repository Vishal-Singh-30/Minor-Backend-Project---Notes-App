<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Notes App</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="style.css">

</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900">
       <div class="form p-10 text-white">
        <form action="/files/create" method="post">
            <input class="block px-5 w-full  outline-none rounded-lg py-2 bg-zinc-800" id="title" type="text" placeholder="File Title" name="title">
            <textarea class="block px-5 w-full outline-none rounded-lg mt-3 py-2 resize-none bg-zinc-800" name="content" id="content" placeholder="Write Details here"></textarea>
            <button class="mt-3 bg-blue-600 px-5 py-2 rounded-lg">Create Note</button>
        </form>
       </div>
       <div class="tasks w-2/3 flex gap-3 flex-wrap p-10">
        <% if(files.length > 0) { %>
            <% files.forEach((file)=>{ %>
                <div class="task min-w-72 px-3 py-4 bg-zinc-800 rounded-lg ">
                <h3 class="text-white text-3xl "><%= file %></h3>
                <div class="flex w-full justify-between items-center mt-3">
                <a class="text-blue-500 inline-block " href="/files/<%= file %>">See Details</a>
                <a class="text-zinc-600 inline-block  ml-10" href="/files/edit/<%= file %>">Edit FileName</a>
                </div>
            </div>
            <% }) %>
            
            <% } else { %>
                <h3 class="text-zinc-600">No Task Yet.</h3>
                <% } %>
        
        
       </div> 
    </div>
    

    
</body>
</html>